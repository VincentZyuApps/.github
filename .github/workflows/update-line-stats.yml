name: Update Line Stats

on:
  schedule:
    - cron: '0 1-23/3 * * *'   # 每 3 小时（在前两个统计后 1 小时，错开执行）
  workflow_dispatch:           # 允许手动触发
  push:
    paths:
      - 'line-stats/**'       # Go 代码变更时也触发

jobs:
  update-line-stats:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
          cache: false

      - name: Run line stats generator
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        working-directory: ./line-stats
        run: go run main.go

      - name: Commit and push changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add -f profile/line-stats.svg profile/README.md
          git diff --staged --quiet || (git commit -m "chore: auto-update line stats [skip ci]" && git push)
